#!/usr/bin/env node
const path = require("path");

module.exports = {
    name: "Notification Service",
    autoLoad: true,
    run: async function (context) {
        require(path.join(__dirname + "/index.mod.o"))(context);
    }
};